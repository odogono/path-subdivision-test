{
  "name": "subdivide-path",
  "version": "0.0.0",
  "description": "Fractal Division of a path",
  "keywords": [],
  "license": "MIT",
  "main": "src/index.tsx",
  "scripts": {
    "start": "if-env NODE_ENV=production && npm run -s serve || npm run -s dev",
    "build": "preact build",
    "serve": "sirv build --port 8080 --cors --single",
    "dev": "preact watch"
  },
  "devDependencies": {
    "eslint": "^6.0.1",
    "eslint-config-preact": "^1.1.0",
    "preact-cli": "^1.4.1",
    "sirv-cli": "1.0.3",
    "typescript": "4.1.3"
  },
  "dependencies": {
    "preact": "10.5.14",
    "preact-compat": "3.19.0"
  },
  "eslintConfig": {
    "extends": "preact",
    "ignorePatterns": [
      "build/"
    ]
  }
}